{
  "name": "redux-effects-media",
  "description": "Effect driver for listening to media query changes in javascript",
  "repository": "git://github.com/redux-effects/redux-effects-media.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/foreach-obj": "^1.1.1",
    "matchmedia-polyfill": "^0.3.0"
  },
  "devDependencies": {
    "@f/combine-reducers": "^1.2.1",
    "@f/handle-actions": "^1.1.2",
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-preset-es2015": "^6.1.2",
    "babel-preset-stage-2": "^6.5.0",
    "babel-tape-runner": "^2.0.1",
    "babelify": "^7.3.0",
    "redux": "^3.5.2",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.2.0"
  }
}
